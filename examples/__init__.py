"""Runnable examples for the creem SDK.

Each file is self-contained: set the CREEM_API_KEY environment variable
(test keys target the sandbox) and run with ``python examples/<name>.py``.
The webhook example needs FastAPI and uvicorn (in the dev extras).
"""

from __future__ import annotations
